// pointsConfig.js
// Single place to configure how many points a stamp is worth.
// Keep it simple for now; later this can load from remote config or environment.

export function getPointsPerStamp() {
  // Default: 2 points per stamp
  return 2;
}
